#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

//Observations:
/*
 XOR -> if (same inputs) 0
        else 1
*/

void solve(){
    int n;cin >> n;
    vector<int> a(n);
    for(auto& x : a) cin >> x;

}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll t;cin >> t;
    while(t--){
        solve();
    }
    return 0;
}